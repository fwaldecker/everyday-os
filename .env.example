# ========================================
# Everyday-OS Environment Configuration
# ========================================
# Quick Start:
# 1. Copy this file to .env
# 2. Update all REQUIRED variables
# 3. Run: python start_services.py

# ========================================
# REQUIRED CONFIGURATION
# ========================================

# --- Domain Configuration ---
# Your domain name (e.g., example.com)
# Services will be available at:
#   - n8n.yourdomain.com
#   - minio-console.yourdomain.com
#   - nca.yourdomain.com
BASE_DOMAIN=yourdomain.com

# Email for Let's Encrypt SSL certificates
LETSENCRYPT_EMAIL=admin@yourdomain.com

# --- Database ---
# PostgreSQL password for n8n database
# Generate secure password with: openssl rand -hex 32
POSTGRES_PASSWORD=changeme-use-strong-password

# --- n8n Security ---
# Encryption key for n8n credentials
# Generate with: openssl rand -hex 32
N8N_ENCRYPTION_KEY=changeme-generate-random-hex-string

# JWT secret for n8n user sessions
# Generate with: openssl rand -hex 32
N8N_USER_MANAGEMENT_JWT_SECRET=changeme-generate-random-hex-string

# --- MinIO Object Storage ---
# Admin credentials (minimum 8 characters)
# Used by NCA Toolkit for file storage
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme-use-strong-password

# --- NCA Toolkit ---
# API key for NCA Toolkit authentication
# Generate with: openssl rand -hex 24
NCA_API_KEY=changeme-generate-api-key

# ========================================
# OPTIONAL CONFIGURATION
# ========================================

# --- AI Services ---
# Uncomment and add your API keys if using AI features

# OpenAI API key (for AI features in n8n and NCA)
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Anthropic API key (for Claude in n8n and NCA)
# Get from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-...

# --- Email Notifications ---
# Configure if you want n8n to send email notifications

# SMTP settings for n8n
# N8N_SMTP_HOST=smtp.gmail.com
# N8N_SMTP_PORT=587
# N8N_SMTP_USER=your-email@gmail.com
# N8N_SMTP_PASS=your-app-specific-password
# N8N_SMTP_SENDER=your-email@gmail.com
# N8N_SMTP_SSL=true