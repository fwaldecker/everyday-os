# Change the name of this file to .env after updating it!

############
# [REQUIRED] Server Configuration
# Update these three values before deployment
############

PROTOCOL=https
BASE_DOMAIN=yourdomain.com
SERVER_IP=your.server.ip.address

############
# [REQUIRED] n8n Credentials
# Generate both values using: openssl rand -hex 32
# Available on Linux/Mac by default, or use Git Bash on Windows
# Alternative: python -c "import secrets; print(secrets.token_hex(32))"
############

N8N_ENCRYPTION_KEY=your-n8n-encryption-key  # generate via `openssl rand -hex 32`
N8N_USER_MANAGEMENT_JWT_SECRET=your-n8n-jwt-secret  # generate via `openssl rand -hex 32`

############
# [REQUIRED] Database Password
# Generate a secure password, avoid @ symbol for connection string compatibility
############

POSTGRES_PASSWORD=your-secure-postgres-password  # generate secure password, avoid @ symbol

############
# [REQUIRED] Neo4j Authentication
# Neo4j graph database (currently not used by any workflows, but available)
# Replace with your chosen username and password
# Format: username/password (keep the "/" separator)
############

NEO4J_AUTH=neo4j/your-secure-neo4j-password  # format: username/password
NEO4J_PASSWORD=your-secure-neo4j-password  # same password as above

############
# [REQUIRED] MinIO Object Storage
# Credentials for S3-compatible storage used by NCA Toolkit
############

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=your-secure-minio-password  # minimum 8 characters

############
# [REQUIRED] Security Keys
# Generate secure keys for various services
############

NEXTAUTH_SECRET=your-nextauth-secret  # generate secure value
ENCRYPTION_KEY=your-encryption-key  # generate via `openssl rand -hex 32`
WEBUI_SECRET_KEY=your-webui-secret-key  # generate via `openssl rand -hex 32`
JWT_SECRET_KEY=your-jwt-secret-key  # generate via `openssl rand -hex 32`
SESSION_SECRET=your-session-secret  # generate via `openssl rand -hex 32`

############
# [REQUIRED] Service Hostnames
# These will be subdomains of your BASE_DOMAIN
############

N8N_HOSTNAME=n8n.${BASE_DOMAIN}
WEBUI_HOSTNAME=chat.${BASE_DOMAIN}
NEO4J_HOSTNAME=neo4j.${BASE_DOMAIN}
NCA_HOSTNAME=nca.${BASE_DOMAIN}
MINIO_CONSOLE_HOSTNAME=minio-console.${BASE_DOMAIN}
MINIO_API_HOSTNAME=minio-api.${BASE_DOMAIN}
LETSENCRYPT_EMAIL=admin@${BASE_DOMAIN}

############
# [OPTIONAL] API Keys
# Add your API keys here to enable AI features in Open WebUI and NCA Toolkit
############

OPENAI_API_KEY=
ANTHROPIC_API_KEY=

############
# [OPTIONAL] SMTP Configuration for n8n
# Configure email for password resets and notifications
############

# SMTP settings (uncomment and configure for email support)
# N8N_SMTP_HOST=smtp.gmail.com
# N8N_SMTP_PORT=587
# N8N_SMTP_USER=your-email@gmail.com
# N8N_SMTP_PASS=your-app-password
# N8N_SMTP_SENDER=your-email@gmail.com
# N8N_SMTP_SSL=false

# Example configurations:
# Gmail: Use app password, not regular password (https://support.google.com/accounts/answer/185833)
# SendGrid: smtp.sendgrid.net, port 587, use API key as password
# Mailgun: smtp.mailgun.org, port 587
# AWS SES: email-smtp.region.amazonaws.com, port 587

############
# [OPTIONAL] Advanced Configuration
# These have sensible defaults and typically don't need changes
############

# Database Configuration (if using external PostgreSQL)
POSTGRES_HOST=postgres
POSTGRES_DB=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres

# NCA Toolkit API Key (for document processing)
NCA_API_KEY=nca-toolkit-default-api-key